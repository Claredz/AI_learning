今天完成了，karpathy 的 micrograd（基本把视频看完了），虽然之前大概了解了反向传播、神经网络的原理，但是代码实现一直没有尝试，毕竟没有系统学习过 python, 很明显发现在语法方面有问题，尤其是 class, 这应该是一个很神奇的东西，我想我应该再仔细看看。

说回 micrograd, 我们在干什么，从数学的视角，我们完成一个数域（Value）以及上面的运算（加减乘除幂）
	虽然因为顺序错了的报错还是太好笑了，如果不加那几行代码，Value 就是非交换的了，梦回 lean 4 了

更重要的是 grad, 也就是求导：这是非常让我惊讶的（我没有意识到的），求导不止是像是 tanh 和它的导数，还有过于自然被我忘记的运算规律，这些作为了运算 class 的固有方法在代码实现里面，结构化，很好很好。

还有 MLP 的实现，用 class 串联了神经元 nn，一层 layer, 以及最后的 mlp.

我之后再仔细看看 karpathy 的仓库，里面貌似还有些其他东西可以学习

